# Construction Artifacts

Runtime-populated artifacts captured during development.

## Subdirectories

- **Design-Decisions/** — architectural choices made during issues (one file per decision)
- **Test-Plans/** — test strategy documents per feature
- **Tech-Debt/** — deferred work and known shortcuts

These directories are populated as work happens via the runtime artifact triggers in `.claude/rules/06-runtime-triggers.md`.
